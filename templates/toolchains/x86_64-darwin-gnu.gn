ToolchainWithLint("x86_64-darwin-gnu") {
  toolchain_type = "gcc"

  cc = "gcc"
  cxx = "g++"
  ar = "ar"
  ld = cxx

  source_extensions_ld = ["ld"]

  toolchain_args = {
    current_cpu = "x86_64"
    current_os = "mac"
    current_platform = "x86_64-darwin-gnu"
  }
}
